# Installation
```shell
git clone https://github.com/unlocomqx/nginx-docker-ssl-proxy.git nginx-ssl
cd nginx-ssl
docker-compose up -d
// test http://localhost
// start your dev server on port 8081 (can be changed in /build/nginx/etc/nginx/conf.d/nginx.conf)
// test https://local.dev
```

